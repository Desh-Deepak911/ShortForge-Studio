/** Minimum resolved duration for any timeline media item (ms). */
export const SCENE_MEDIA_MIN_ITEM_DURATION_MS = 500;

/** Experimental notice — render once on StudioTimeline when the gate is ON and scenes exist. */
export const SCENE_MEDIA_TIMELINE_EXPERIMENTAL_NOTICE =
  "Multiple images per scene. Preview and export follow the Scene Media Timeline with hard-cut item boundaries. Intra-scene transition effects are editable metadata awaiting Preview/Export integration (Sprint 9B/9C).";

/** Per-media Inspector notice — Preview follows timeline when the gate is ON. */
export const SCENE_MEDIA_ITEM_INSPECTOR_PREVIEW_NOTICE =
  "Main Preview follows Scene Media Timeline playback for the selected scene.";
