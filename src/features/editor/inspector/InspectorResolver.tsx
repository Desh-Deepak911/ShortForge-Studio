"use client";

import InspectorTabShell from "./InspectorTabShell";

/**
 * Selection-driven inspector — Scene and Project tabs with pinned sync UI above.
 */
export default function InspectorResolver() {
  return <InspectorTabShell />;
}
