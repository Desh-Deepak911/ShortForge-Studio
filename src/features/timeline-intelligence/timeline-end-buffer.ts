/**
 * Frozen ExportManifest / master-timeline end-buffer authority (ms).
 * Kept in a leaf module so headless packaging can import the constant without
 * pulling buildMasterTimeline → voiceover → speech-style React panels.
 */
export const TIMELINE_END_BUFFER_MS = 400;
