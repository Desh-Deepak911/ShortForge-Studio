/**
 * @deprecated Import from `./entity-types` instead.
 * Retained temporarily for architecture scaffold compatibility.
 */
export type {
  EntityType as EntityKind,
  ResolvedEntity as EntityReference,
  EntityResolution as EntityResolutionResult,
} from "./entity-types";
