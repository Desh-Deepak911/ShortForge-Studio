/**
 * Targeted R2 harness context — reuses the live matrix session/context shape.
 */

export type {
  R2LiveMatrixContext,
  R2LiveSessionState,
} from "../r2-live/types";

export type { R2LiveMatrixContext as R2TargetedMatrixContext } from "../r2-live/types";
export type { R2LiveSessionState as R2TargetedSessionState } from "../r2-live/types";
